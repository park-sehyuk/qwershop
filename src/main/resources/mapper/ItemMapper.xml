<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.qwerShop.mapper.ItemMapper">

    <select id="getItemList" resultType="com.example.qwerShop.dto.ItemDTO">
        SELECT i.item_id, i.item_name, i.item_brand, i.item_price,
        i.item_category, i.item_type, ii.item_url
        FROM item i
        LEFT JOIN item_img ii ON i.item_id = ii.item_id
        WHERE ii.is_main = 'Y'
        <if test="type != null">
            AND i.item_type = #{type}
        </if>
    </select>

</mapper>